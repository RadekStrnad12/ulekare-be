APP_ENV=dev
APP_SECRET=ChangeMeSecret
APP_SHARE_DIR=var/share

DEFAULT_URI=http://localhost

DATABASE_URL="mysql://root:root@127.0.0.1:8889/ulekare?server_version=8.0.35&charset=utf8mb4"

# CORS configuration (example defaults)
# Adjust for your environment. For development, set this to your FE origin.
CORS_ALLOW_ORIGIN=http://localhost:5173
CORS_ALLOW_METHODS=GET, POST, PUT, DELETE, OPTIONS
CORS_ALLOW_HEADERS=Content-Type, Authorization
CORS_ALLOW_CREDENTIALS=false
CORS_EXPOSE_HEADERS=
CORS_MAX_AGE=86400
